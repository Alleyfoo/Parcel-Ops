# Handoff — 2026-06-01 (session 3)

## What was completed this session

### Live Gemini-backed HS classification showcase (commit dd2668d)

Replaced the mocked LLM column in the Data Pipeline tab with a real Gemini
call, mirroring the sql-editor pattern (session-only API key, stdlib urllib
against the OpenAI-compat Gemini endpoint, no new pip deps).

**Files added or changed**
- `parcel_ops_llm/__init__.py` (new) — Gemini client, config loader, connection probe
- `llm_test_cases.json` (new) — 12 curated cases with gold HS codes
- `llm_classifier.py` (rewritten) — regex classifier (deterministic, intentionally
  imperfect), real LLM classifier, gold-answer evaluator
- `app.py` (modified) — pipeline tab now has the LLM config panel, Run-all
  button with progress bar, per-case expanders scored against gold codes

**Important rename** (commit f6d9cb1): the package was originally named `llm/`,
which is a well-known third-party Python package (Simon Willison LLM CLI).
On Streamlit Cloud the runtime could pick up the third-party package instead
of the local one and the `from llm import …` would fail with ImportError.
Renamed to `parcel_ops_llm/`. If you see ImportError on `from llm import …`
in any future log, this is the same root cause — keep the package name unique.

**Smoke test results** (Playwright + HTTP checks)
- HTTP 200 on `streamlit run app.py` (port 8512)
- All 4 tabs render (Dashboard, Data Pipeline, API, Documents)
- LLM vs Regex header found inside the pipeline tab
- 12 case expanders render in the DOM
- "Run LLM on all cases" button is present and **correctly disabled** when no
  API key is set
- "Test connection" button gracefully returns "Probe: No API key configured"
- All expected text present in DOM body (62k chars): "LLM vs Regex",
  "Test Cases", "Run LLM on all cases", "Gemini API key", "Software license key",
  "Elektronische Spannungsregler", "HS Code:"
- 0 console errors, 0 page errors
- Note: headless Playwright screenshots come back blank — this is a known
  streamlit+Playwright headless rendering issue, not a code bug. The DOM is
  fully populated and a real browser renders fine.

**Test case mix** (12 cases, externally editable in `llm_test_cases.json`)
- 4 ambiguous products (LLM expected to win)
- 3 multilingual descriptions (DE, ZH, ES) (LLM expected to win)
- 1 multi-component kit (LLM expected to win via GRI 3(b))
- 2 clean keyword cases (regex expected to win)
- 2 edge cases (software on paper, refurbished laptop) — regex expected to win,
  these exist specifically to surface LLM hallucination

**Regex baseline** verified offline: 4/12 = 33% correct. The 8 LLM-expected
cases are pending real Gemini calls (will be filled in when the user clicks
"Run LLM on all cases" with a key configured).

## Uncommitted changes

None. Tree is clean. The prior session's "uncommitted handout 001 work"
mentioned in the previous handoff is no longer in the working tree — either
committed separately or rolled in. Last commit on main before this session
was 71e879a.

## Known issues

- The `initial_sidebar_state="collapsed"` page config means the LLM config
  panel lives inside the pipeline tab (not the streamlit sidebar). This was
  intentional to keep the design system intact. If the user prefers a true
  sidebar, the config panel can be moved by changing `initial_sidebar_state`
  to `"expanded"` and wrapping the panel in `with st.sidebar:`.
- Free-tier Gemini has per-minute rate limits. Running all 12 cases back-to-
  back could hit 429. The client does not currently retry; if a 429 fires the
  case will show "Call failed: HTTP 429: …" in the LLM column.
- The probe cache lives in `st.session_state` only — closing the tab clears
  it. This is intentional (matches sql-editor pattern).
- The model list is hardcoded to the free-tier-friendly defaults. Newer
  models can be added by editing `DEFAULT_GEMINI_MODELS` in `llm/__init__.py`.

## Recommended next tasks

1. **Run the showcase with a real key** — enter a Gemini free-tier key from
   aistudio.google.com in the LLM panel on the Data Pipeline tab and click
   "Run LLM on all cases". Capture the per-case accuracy and report back so
   the next agent can record actual LLM win/loss numbers in this handoff.

2. **Optional: add a per-case retry** for 429s in `llm/__init__.py::chat_completion`
   (exponential backoff, max 2 retries). Free-tier rate limits are tight
   enough that this will probably be needed if you run the full 12 cases.

3. **Optional: add a CSV export** of the per-case results table (case_id,
   description, gold_hs_code, regex_hs_code, llm_hs_code, regex_correct,
   llm_correct, latency_ms). Useful for sharing results outside the dashboard.

4. **Optional: create handout 002 in `docs/manager/workspace/handouts/`** to
   capture this work in the Manager Summoner tracker, mirroring the format
   of `handout_001_done.md`. The project workflow calls for that, but the
   user did not request it for this feature.

## Design decisions made

- `llm/` is a package (not a module) so future model clients (Claude, Ollama)
  can be added as siblings without restructuring.
- Config precedence matches sql-editor: `env > st.session_state >
  st.secrets > yaml`. No `config.yaml` is read in this project yet; the
  loader handles the missing case cleanly.
- The LLM config panel is a card inside the pipeline tab, not a sidebar
  widget, because the page config has `initial_sidebar_state="collapsed"`.
  The card uses the existing `.pipeline-card` design system class so it
  blends in with the rest of the tab.
- Regex rules are intentionally basic and ordered so that the **first match
  wins**. This mirrors what a real production regex pipeline would do and
  gives the LLM a fair target.
- Test cases are in a JSON file (not inlined) so they can be reused by a
  future CLI eval script, version-controlled separately, and edited without
  touching Python.
- The "expected_winner" field on each case is a UI hint, not a hard
  assertion. The gold code is the only ground truth.
- No new pip dependencies. The `google-generativeai` SDK would have been
  ~10MB; the stdlib urllib path is ~50 lines and works on Streamlit Cloud
  out of the box.

## Repo genome note

The genome unit for `dashboard.yaml` (created in session 2) covers `app.py`
and `parcel_schema.py`. This session added new units of behaviour
(`llm/`, `llm_classifier.py`, `llm_test_cases.json`). Consider creating:
- `repo_genome/units/llm.yaml` (client + config loader, layer: integration)
- `repo_genome/units/llm_classifier.yaml` (regex rules + test cases, layer: domain)
and re-running `python scripts/build_repo_genome.py --write` so the genome
stays useful.
